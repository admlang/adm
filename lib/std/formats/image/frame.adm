partial module std.formats.image {
	use (
		std.math.geom
		std.gfx.color
	)

	type ImageFrame {
		image     Image
		length    duration
		offset    geom.Point
		blendMode color.BlendMode
	}

	type ImageSequence {
		loopCount int          = 0 // 0 means infinite loop
		frames    ImageFrame[]
	}
}
