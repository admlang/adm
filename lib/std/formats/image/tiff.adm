partial module std.formats.image {
	use (
		std.io
		std.bytes
	)

	// TIFF is the TIFF codec. Decoding and encoding are not implemented yet;
	// matchSignature reports that nothing matches.
	@codec("Tagged Image File Format")
	type TIFF {
		mimeType string[] {
			def get() string[] {
				return string["image/tiff"]
			}
		}
		extension string[] {
			def get() string[] {
				return string[".tif", ".tiff"]
			}
		}

		// matchSignature reports whether the reader starts with a TIFF signature.
		// Not implemented yet: always false.
		def matchSignature(r io.Reader) bool {
			return false
		}

		// info reads the image's dimensions and format from the bytes. Fails, as
		// decoding is not implemented yet.
		def info(data byte[]) !ImageInfo {
			return info(new bytes.Buffer(data))
		}

		// info reads the image's dimensions and format from the reader. Fails, as
		// decoding is not implemented yet.
		def info(reader io.Reader) !ImageInfo {
			fail "not implemented"
		}

		// decode reads a TIFF image from the bytes. Fails, as decoding is not
		// implemented yet.
		def decode(data byte[]) !Image {
			return decode(new bytes.Buffer(data))
		}

		// decode reads a TIFF image from the reader. Fails, as decoding is not
		// implemented yet.
		def decode(reader io.Reader) !Image {
			fail "not implemented"
		}

		// encode renders the image as TIFF. Not implemented yet: returns no bytes.
		def encode() byte[] {}

		// encode writes the image as TIFF to the writer. Not implemented yet; it
		// cannot fail.
		def encode(writer io.Writer) !none {}
	}
}
