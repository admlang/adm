partial module std.i18n {
	// Calendar is the calendar system dates are counted in when they are formatted
	// or parsed for a locale.
	enum Calendar {
		Gregorian
		Buddhist
		Islamic
		Hebrew
		Japanese
	}
}
