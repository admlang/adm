partial module std.i18n {
	// DateTimeFormat is how a locale writes dates and times: the date pattern, the
	// time pattern and the calendar they count in.
	struct DateTimeFormat {
		datePattern string
		timePattern string
		calendar    Calendar
	}
}
