partial module std.i18n {
	// Locale is a language as used in a region and written in a script, with any
	// variants; it selects the formats and rules the module applies.
	struct Locale {
		language Language
		script   Script
		region   Region
		variants string[]
	}
}
