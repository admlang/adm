partial module std.i18n {
	// NumberFormat is how a locale writes numbers: the decimal separator, the
	// grouping separator and the sizes of the digit groups it separates.
	struct NumberFormat {
		decimalSeparator  string
		groupingSeparator string
		groupingPattern   int[]
	}
}
