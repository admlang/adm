partial module std.io {
	def readFull(r Reader, buf byte[]) !int {
	    let min = buf.len()
	    let n = 0
	    for n < min {
	        let nn int
	        nn = try r.read(buf[n:])
	        n += nn
	    }
	    return n
	}
}
