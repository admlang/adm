partial module std.metrics {
	// A toggle keeps a boolean value
	type Toggle {
		internal atomic val bool

		def kind() string {
			return "toggle"
		}

		def update(value bool) {
			val = value
		}

		def value() bool {
			return val
		}

		def str() string {
			return sprintf("Metric[%s](%s)", kind(), val ?? ("TRUE", "FALSE"))
		}
	}
}
