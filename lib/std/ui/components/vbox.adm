partial module std.ui.components {
	// Stacks its children top to bottom, gap apart.
	component VBox {
		style {

		}
		view {

		}
		type {
		    // Gap is the minimum spacing between adjacent children
			gap int
            horizontal Distribution = Distribution.Center
            vertical Align = Align.Start
            reverse bool = false
		}
	}
}
